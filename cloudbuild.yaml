# Cloud Build 配置 - 自動化構建和部署
# 使用方式: gcloud builds submit --config cloudbuild.yaml

steps:
  # 運行後端測試（必須通過才能繼續構建）
  - name: 'python:3.11'
    id: 'backend-tests'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e  # 遇到錯誤立即退出
        cd backend
        echo "📦 安裝依賴..."
        pip install --upgrade pip
        pip install -r requirements.txt
        echo "🧪 運行測試..."
        python manage.py test --verbosity=2 --noinput
        if [ $? -ne 0 ]; then
          echo "❌ 測試失敗！部署已中止。"
          exit 1
        fi
        echo "✅ 所有測試已通過！繼續部署..."
    env:
      - 'DATABASE_ENGINE=django.db.backends.sqlite3'
      - 'DJANGO_SECRET_KEY=${_DJANGO_SECRET_KEY:-test-secret-key-for-ci-cd}'
      - 'DJANGO_DEBUG=False'

  # 構建 Docker 映像
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$SHORT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'
      - '.'

  # 推送映像到 Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$SHORT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'

  # 部署到 Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$SHORT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--max-instances'
      - '10'
      - '--timeout'
      - '300'
      - '--set-cloudsql-instances'
      - '${_CLOUD_SQL_CONNECTION_NAME}'
      - '--set-env-vars'
      - 'DJANGO_DEBUG=False'

# 替換變數（使用 --substitutions 參數提供）
substitutions:
  _SERVICE_NAME: '9jang-backend'
  _REGION: 'asia-east1'
  _CLOUD_SQL_CONNECTION_NAME: ''  # 例如: project:region:instance
  _DJANGO_SECRET_KEY: ''  # Django 密鑰（可選，測試時會使用默認值）

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'

