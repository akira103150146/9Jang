# ============================================
# 後端環境變數配置範例
# ============================================
# 複製此文件為 .env 並填入實際值

# ============================================
# 環境設定
# ============================================
ENVIRONMENT=development

# ============================================
# Django 安全設定
# ============================================
# 請在生產環境中生成新的 SECRET_KEY
DJANGO_SECRET_KEY=django-insecure-k#&ll@igxrfb*g=la9c9g=r_@thy06_10wywdsysaxzr2v!@el
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# 伺服器配置
# ============================================
BACKEND_HOST=localhost
BACKEND_PORT=8000
BACKEND_URL=http://localhost:8000

# ============================================
# 資料庫配置
# ============================================
# SQLite（開發環境）
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# PostgreSQL（生產環境範例）
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=your_db_name
# DATABASE_USER=your_db_user
# DATABASE_PASSWORD=your_db_password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# ============================================
# CORS 設定
# ============================================
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000

# ============================================
# CSRF 設定
# ============================================
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000

# ============================================
# JWT 設定
# ============================================
JWT_ACCESS_TOKEN_LIFETIME_HOURS=1
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7
JWT_ROTATE_REFRESH_TOKENS=True
JWT_BLACKLIST_AFTER_ROTATION=True

# ============================================
# Session 設定
# ============================================
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_HTTPONLY=True

# ============================================
# 媒體檔案設定
# ============================================
MEDIA_URL=/media/
MEDIA_ROOT=media

# ============================================
# 時區設定
# ============================================
TIME_ZONE=Asia/Taipei
LANGUAGE_CODE=zh-hant
